/*
 * Renders an animation to communicate the fact that the program is processing information
 */
function renderLoadingAnimation() {
  var loadingAnimationContainer = document.getElementById("loading-animation-container");
  loadingAnimationContainer.style.display = "block";

}